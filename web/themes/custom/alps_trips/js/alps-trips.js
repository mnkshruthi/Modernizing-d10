/**
 * @file
 * alps_trips behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.alpsTrips = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
